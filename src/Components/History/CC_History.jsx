import React, { useEffect, useState } from 'react';
import './CC_History.css';

export function CC_History(){
    const [historyList, setHistoryList] = useState([]);
    // Fetch history list on component mount
    
}